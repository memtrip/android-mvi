<?xml version="1.0" encoding="utf-8"?>

<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/viewButtonPrimaryBackgroundSelected">

    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@color/viewUtilDrawableMask" />
            <corners android:radius="@dimen/viewButtonBorderRadius" />
        </shape>
    </item>

    <item android:drawable="@drawable/view_button_primary_background_rounded" />

</ripple>